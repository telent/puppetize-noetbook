# This directory is for config fragments and stuff that should not be
# published on github.  They will be included with the puppet 'import'
# directive.  If you make sure your filenames match *.local.pp, they
# will be .gitignored


